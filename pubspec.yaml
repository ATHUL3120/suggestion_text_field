name: suggestion_input_field
description: Suggestion_input_field is a Flutter package that provides a customizable input field with suggestions for faster and more accurate user input. It is ideal for forms, search bars, and any other input scenarios where suggestions can enhance the user experience.
version: 0.0.2
homepage: https://github.com/ChegzDev/suggestion_input_field
repository: https://github.com/ChegzDev/suggestion_input_field

environment:
  sdk: '>=2.19.4 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
flutter:

